{{!< default}}
{{!-- The tag above means - insert everything in this file into the {body} of the default.hbs template --}}

{{#author}}
<div class = "centre-hero">
    <header>
        <h1>{{@site.title}}</h1>
        <div>{{@site.description}}</div>
    </header>

    <main>
        <code>There are no pages for authors</code>
        <p>At the moment, there is no page for {{name}} in this theme. Check back later to see if author pages are added to this theme.</p>
    </main>
</div>
{{/author}}
