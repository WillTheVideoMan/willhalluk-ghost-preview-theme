{{!< default}}
{{!-- The tag above means: insert everything in this file
into the {body} of the default.hbs template --}}

<div class = "centre-hero">
    <header>
        <h1>{{@site.title}}</h1>
        <div>{{@site.description}}</div>
    </header>

    <main>
        <code>This is just a preview</code>
        <p>Make your changes in Ghost and they will appear on the main site in just a jiffy.</p>
    </main>
</div>
