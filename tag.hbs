{{!< default}}
{{!-- The tag above means - insert everything in this file into the {body} of the default.hbs template --}}

{{#tag}}

<div class = "centre-hero">
    <header>
        <h1>{{@site.title}}</h1>
        <div>{{@site.description}}</div>
    </header>

    <main>
        <code>This is just a preview</code>
        <p>At the moment, there is no page for the {{name}} tag in this preview theme. Visit the main site to view the tag page.</p>
    </main>
</div>

{{/tag}}